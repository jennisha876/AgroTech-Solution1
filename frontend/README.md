# Frontend Folder

The active frontend source stays in the repository root:

- `src/`
- `public/`
- `index.html`

This folder exists for deployment and workspace compatibility when a dedicated `frontend` directory is expected.

Use the root `package.json` scripts to run or build the frontend.
